import { SearchBtn } from "./index";

export default {
  title: "SearchBtn",
  component: SearchBtn,
};

const Template = (arg) => <SearchBtn {...arg} />;

export const Default = Template.bind({});

Default.args = {};
