import { ContextMenuIcon } from "./index";

export default {
  title: "ContextMenuIcon",
  component: ContextMenuIcon,
};

const Template = (arg) => <ContextMenuIcon {...arg} />;

export const Default = Template.bind({});

Default.args = {};
