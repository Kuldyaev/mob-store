import { BackBtn } from "./index";

export default {
  title: "BackBtn",
  component: BackBtn,
};

const Template = (arg) => <BackBtn {...arg} />;

export const Default = Template.bind({});

Default.args = {};
