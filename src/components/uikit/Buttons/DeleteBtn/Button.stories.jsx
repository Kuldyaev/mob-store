import { DeleteBtn } from "./index";

export default {
  title: "DeleteBtn",
  component: DeleteBtn,
};

const Template = (arg) => <DeleteBtn {...arg} />;

export const Default = Template.bind({});

Default.args = {};
